fichier ldif: 

dn: olcDatabase={1}mdb,cn=config
changetype: modify
replace: olcAccess
olcAccess: {0} to attrs=userPassword 
 by self write 
 by anonymous auth 
 by * none
-
replace: olcAccess
olcAccess: {1} to attrs=shadowLastChange 
 by self write 
 by * read
-
replace: olcAccess
olcAccess: {2} to attrs=telephoneNumber 
 by dn="cn=admin,dc=annuaire" write 
 by self write 
 by users read 
 by * none 
-
replace: olcAccess
olcAccess: {3} to * 
 by dn="cn=admin,dc=annuaire" write 
 by users read 
 by * none 



(ne pas oublier les esapce) 

pour vérif: ```sudo ldapmodify -v -n -f acl.ldif```

reagrder que t'as bien les esapce entre les by sinon rajouter

commande pour add les règles: 

```sudo ldapmodify -Q -Y EXTERNAL -H ldapi:/// -f acl.ldif ```

